/**
 * Relative-to-origin location paths, i.e. they should all begin with a forward slash as in "/login".
 */
export const LOCATION_PATHS = {
    home: "/",
    services: "/services",
    login: "/login"
}