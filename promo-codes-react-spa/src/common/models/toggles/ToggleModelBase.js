export default class ToggleModelBase {
    /**
     * 
     * @param {Boolean} isOn Says whether or not toggle is on. Boolean true means it is on.
     */
    constructor(isOn) {
        this.isOn = isOn;
    }
};
